{% extends 'base.html.twig' %}

{% block title %}New SchoolYear{% endblock %}

{% block body %}
    <h1>Create new SchoolYear</h1>

        {{ include('school_year/_form.html.twig') }}

    {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_TEACHER') %}
        <a href="{{ path('school_year_index') }}">back to list</a>
    {% endif %}

{% endblock %}

